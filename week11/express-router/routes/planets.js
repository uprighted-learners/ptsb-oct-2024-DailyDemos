import express from "express";
const router = express.Router();

router.get("/", () => {
  // code here for endpoint
});

router.get("/:id", () => {
  // code here for endpoint
});

router.post("/", () => {
  // code here for endpoint
});

router.put("/", () => {
  // code here for endpoint
});

router.patch("/", () => {
  // code here for endpoint
});

export default router;
